clear;clc;
x=input('X Deðerini Giriniz:');
if 1<=x<10
    y=exp(-x-1)/log10(x)+log(x);
elseif x>=10
    y=sin(3*x)/sqrt(3*pi);
else 
    x^(-exp-x)+sqrt(x^3*abs(x));
end
    fprintf('y=%g\n',y);